#TODO: this file should be in private directory
# Remote git repository URL (user's fork)
REMOTE_URL="git@github.com:doryashar/my-env"
ENV_DIR="${HOME}/env"
export BW_EMAIL="${BW_EMAIL:-dor.yashar@gmail.com}"

# Private encrypted repository URL
PRIVATE_URL="git@github.com:doryashar/encrypted.git"

# Upstream URL for infrastructure updates (when using a fork)
UPSTREAM_URL="git@github.com:doryashar/my-env.git"

# Display settings
SHOW_DUFF=off
SHOW_NEOFETCH=on

# Sync timing (days)
CHECK_INTERVAL_DAYS="${CHECK_INTERVAL_DAYS:-1}"
SYNC_INTERVAL_DAYS="${SYNC_INTERVAL_DAYS:-7}"

# Merge conflict resolution strategy: ask, local, remote, rename, ignore
DEFAULT_CONFLICT_STRATEGY="${DEFAULT_CONFLICT_STRATEGY:-ask}"
